---
"@indev42/zwidget": patch
---

Match Zoho's accepted widget zip layout with root `plugin-manifest.json` and `app/` entries, and warn when builds approach Zoho's observed archive entry-count or file-size rejection ranges.
